local wk = require("which-key")

wk.setup()
-- below is an attempt to replace "prefix" with meaningful group names, but it is not working yet
-- wk.register({
-- f = { name = "Find & Search", a = { "<cmd>Telescope find_files<cr>", "Find File" } },
-- })
